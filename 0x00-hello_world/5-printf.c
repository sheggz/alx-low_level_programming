#include <stdio.h>

/**
 * main - prints a string using the printf() function
 *
 * Return: 0 if there program runs without error
*/

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
